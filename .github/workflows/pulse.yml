name: Bullions Engine Pulse

on:
  schedule:
    - cron: "* * * * *"
  workflow_dispatch:

jobs:
  pulse:
    runs-on: ubuntu-latest
    steps:
      - name: Call backend engine pulse
        run: curl -fsS https://bullions6x.com/api/cron/engine-pulse
