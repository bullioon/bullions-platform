name: Bullions Pulse

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  pulse:
    runs-on: ubuntu-latest
    steps:
      - name: Pulse leaderboard
        run: curl -s https://bullions6x.com/api/leaderboard/pulse

      - name: Pulse copy engine
        run: curl -s https://bullions6x.com/api/engine/pulse
